services:
  - type: web
    name: user-management-api
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/user-management-api-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
